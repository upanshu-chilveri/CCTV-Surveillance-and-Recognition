name: cctv_reid
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.10
  - pip
  - pytorch
  - torchvision
  - pytorch-cuda=11.8   # Change to 12.1 if you have newer hardware/drivers
  - pip:
    - ultralytics>=8.0.0
    - deep-sort-realtime>=1.3.2
    - opencv-python>=4.7.0
    - scikit-learn>=1.2.0
    - matplotlib>=3.6.0
    - seaborn>=0.12.0
    - pandas>=1.5.0
    - tqdm>=4.64.0
    - tensorboard>=2.12.0
    - gdown>=4.7.1